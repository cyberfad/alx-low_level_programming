#include <stdio.h>

/**
 * main - printing single digit numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number;

	for (number = 0; number < 1; number++)
		printf(number);
	printf("\n");
	return (0);
}
