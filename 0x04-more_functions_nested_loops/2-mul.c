#include "main.h"
# include <stdio.h>
/**
 * mul(int a, int b)-  multiplies two object
 * @a: integer to be multiplied
 * @b:  integers to multiply
 * Return: 0 Success
 */

int mul(int a, int b)
{
	return (a * b);
}
